(function (root, factory) {
  const api = factory(); if (typeof module === 'object' && module.exports) module.exports = api; root.WebSCADALiveMeasurementCache = api;
}(typeof globalThis !== 'undefined' ? globalThis : this, function () {
  const KEY = 'webscadaLiveMeasurementCache'; const CACHE_RETENTION_MS = 5 * 60 * 1000; const CACHE_REUSE_MAX_AGE_MS = 45 * 1000; const LIMIT = 2000;
  const storage = () => chrome.storage.session || chrome.storage.local;
  const idOf = row => String(row?.sinsid ?? row?.measurementId ?? ''); const elementOf = row => String(row?.elementName ?? '');
  const timeOf = row => { const value = row?.__time ?? row?.timestamp ?? row?.['MAX(__time)']; const time = new Date(value).getTime(); return Number.isFinite(time) ? time : NaN; };
  const keyOf = row => `${idOf(row)}|${elementOf(row)}`;
  function usable(entry, semantics, now = Date.now(), maxAgeMs = CACHE_REUSE_MAX_AGE_MS) { return entry?.semantics === semantics && Number(entry?.fetchedAt) > now - maxAgeMs && Number.isFinite(timeOf(entry.row)) && now - timeOf(entry.row) <= CACHE_RETENTION_MS; }
  async function read(ids, semantics, options = {}) { const now = typeof options === 'number' ? options : Number(options?.now || Date.now()); const maxAgeMs = options?.forceFresh ? 0 : Number(options?.maxAgeMs || CACHE_REUSE_MAX_AGE_MS); const data = await storage().get(KEY); const entries = Object.values(data[KEY] || {}).filter(entry => usable(entry, semantics, now, maxAgeMs)); const wanted = new Set((ids || []).map(String)); const rows = entries.filter(entry => wanted.has(idOf(entry.row))).map(entry => entry.row); const present = new Set(rows.map(idOf)); return { rows, reusedIds: [...wanted].filter(id => present.has(id)), missingIds: [...wanted].filter(id => !present.has(id)), reuseMaxAgeMs: maxAgeMs }; }
  async function merge(rows, semantics, source = 'network', fetchedAt = Date.now()) { const data = await storage().get(KEY); const cache = data[KEY] || {}; (rows || []).filter(row => idOf(row) && elementOf(row) && Number.isFinite(timeOf(row))).forEach(row => { const key = `${semantics}:${keyOf(row)}`; const existing = cache[key]; if (!existing || timeOf(existing.row) <= timeOf(row)) cache[key] = { row, semantics, source, sourceTimestamp: new Date(timeOf(row)).toISOString(), fetchedAt }; }); const kept = Object.entries(cache).sort((a, b) => Number(b[1].fetchedAt || 0) - Number(a[1].fetchedAt || 0)).slice(0, LIMIT); await storage().set({ [KEY]: Object.fromEntries(kept) }); return kept.length; }
  return { KEY, CACHE_RETENTION_MS, CACHE_REUSE_MAX_AGE_MS, idOf, elementOf, timeOf, keyOf, usable, read, merge, storage };
}));
