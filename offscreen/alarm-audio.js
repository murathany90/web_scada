chrome.runtime.onMessage.addListener((message) => { if (message?.target !== 'offscreen-audio' || message.type !== 'PLAY_ALARM_SOUND') return; const context = new AudioContext(); const oscillator = context.createOscillator(); const gain = context.createGain(); oscillator.frequency.value = message.severity === 'warning' ? 660 : 880; gain.gain.setValueAtTime(.045, context.currentTime); gain.gain.exponentialRampToValueAtTime(.001, context.currentTime + .45); oscillator.connect(gain).connect(context.destination); oscillator.start(); oscillator.stop(context.currentTime + .46); });
