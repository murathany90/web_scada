{
  "name": "webscada-extension",
  "private": true,
  "version": "0.6.13",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "powershell -ExecutionPolicy Bypass -File scripts/build-extension.ps1",
    "build:extension": "npm run build",
    "test:standalone": "powershell -ExecutionPolicy Bypass -File scripts/test-standalone.ps1"
  }
}
